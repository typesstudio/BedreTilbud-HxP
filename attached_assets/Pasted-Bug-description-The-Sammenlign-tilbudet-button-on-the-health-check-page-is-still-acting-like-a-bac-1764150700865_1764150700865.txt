Bug description
The "Sammenlign tilbudet" button on the health check page is still acting like a back button (history.go(-1)).  

Example:
- Health check URL (Indbo):
  /sundhedstjek/4ae81653-1501-4976-b62d-7371af05dfe2

- The correct main comparison URL for this offer:
  /sammenligning/ce529d3c-940b-4157-a9cd-c76e1e225668

Expected behaviour
- The "Sammenlign tilbudet" button should **always** navigate directly to the *Samlet* comparison page for the offer that this health check belongs to:
  /sammenligning/{comparisonId}

- It must **not** depend on browser history (no navigate(-1), history.back(), etc.).
- If a comparisonId does not exist for some reason, the button can either be hidden/disabled, or fall back to a neutral page (e.g. “Se alle bedre tilbud”), but it should never behave like a back button.

Important:  
You already said:
- Backend has been updated so the health check API returns `comparisonId`.
- Frontend transformer was updated to expose `comparisonId` in the health-check view model.
But in the UI the button still behaves like "back", so either:
- comparisonId isn’t actually wired through to the component, OR
- the button is still calling navigate(-1) regardless of comparisonId.

Please fix this end-to-end.

Tasks

1) Verify API actually returns comparisonId
- Call the health check API for the example snapshot with the correct X-User-ID:
  - snapshotId: 4ae81653-1501-4976-b62d-7371af05dfe2

- Confirm the JSON contains something like:
  {
    "comparisonId": "ce529d3c-940b-4157-a9cd-c76e1e225668",
    ...
  }

- If it does NOT, fix the API route so it joins/derives the correct `comparisonId` from the snapshot and company_comparisons tables and returns it.
  - Only change as much as needed; don’t touch other behaviour.

2) Verify transformHealthCheck passes comparisonId through
- Open `client/src/utils/transformHealthCheck.ts`.

- Make sure the transformer that builds the `HealthCheckViewModel` includes a field, e.g.:

  ```ts
  export interface HealthCheckViewModel {
    ...
    comparisonId?: string;
  }

  export function transformHealthCheckResponse(data: HealthCheckApiResponse): HealthCheckViewModel {
    return {
      ...
      comparisonId: data.comparisonId ?? undefined,
    };
  }
